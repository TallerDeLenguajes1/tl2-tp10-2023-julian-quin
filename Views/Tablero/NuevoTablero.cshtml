@using tl2_tp10_2023_julian_quin.ViewModels
@model CrearTableroViewModel
<h2>Nuevo Tablero</h2>
<nav class="mb-4" style="margin-right: 10vw;">
    <ul class="list-unstyled" >
        <li class="d-inline-block"  style="margin-left: 340px;">
            <a class="btn btn-secondary" role="button" aria-pressed="true" asp-controller="Tablero" asp-action="Index" style="color: rgb(255, 255, 255); background-color:rgb(117, 117, 117);">Volver</a>
        </li>
    </ul>
</nav>
<form class="forms_Crear" asp-controller="Tablero" asp-action ="NuevoTablero"> 
    <div class="form-group">
        <label asp-for="@Model.Nombre"></label>
        <input asp-for="@Model.Nombre" class="form-control" />
        <span asp-validation-for="@Model.Nombre" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="@Model.Descripcion"></label>
        <input asp-for="@Model.Descripcion" class="form-control"/>
        <span asp-validation-for="@Model.Descripcion" class="text-danger"></span>
    </div>
     <div class="form-group">
        <label asp-for="@Model.IdUsuarioPropietario"></label>
        <select asp-for="@Model.IdUsuarioPropietario" class="form-control">
            @foreach (var usuario in Model.Usuarios)
            {
                <option value="@usuario.Id">@usuario.NombreDeUsuario</option>
            }
        </select>
        <span asp-validation-for="@Model.IdUsuarioPropietario" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-success">Crear</button>
</form>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
