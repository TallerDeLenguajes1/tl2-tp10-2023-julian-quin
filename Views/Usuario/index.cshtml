@{
    ViewData["Title"] = "Home Page";
}
@model List<Usuario>
@*la vista que espera un modelo del tipo lista de Usuario, es decir se esta definiendo un modelo para la vista*@
@*usamos la sintaxis Razor para definir código C# en la vista*@

<h2>Lista de Usuarios</h2>
<table class="table table-dark">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>ID</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var usuario in Model)
        {
            <tr>
                <td hidden>@usuario.Id</td>
                <td>@usuario.NombreDeUsuario</td>
                <td>@usuario.Id</td>
                <td>
                    <a asp-controller="Usuario" asp-action="ModificarUsuario" asp-route-idUsuario ="@usuario.Id">Editar</a>    @*asp-route-idUsuario // permite mandar el id hacia el get de Modificar (ya que necesita del parametro idUsuario) y asi mostrar el formulario completo de acuerdo al usuario que se cliquee en la tabla de momento para modificar*@
                    @*Etiqueta anchor que dirige a un controlador y luego a un método determinado
                     y pasa un id como parámetro y especifica un nombre para el parámetro enviado*@
                    <a asp-controller="Usuario" asp-action="EliminarUsuario" asp-route-idUsuario="@usuario.Id" onclick="return confirm('¿Estas seguro que quieres eliminar el elemento?')">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>